# Staging Environment

This directory contains the configuration for the staging environment.

## Purpose

The staging environment is used for pre-production testing and validation before changes are deployed to production.

## Configuration

This environment is configured in the `colonia.yaml` file at the repository root.
