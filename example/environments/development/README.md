# Development Environment

This directory contains the configuration for the development environment.

## Purpose

The development environment is used for testing and development purposes before changes are promoted to staging or production.

## Configuration

This environment is configured in the `colonia.yaml` file at the repository root.
