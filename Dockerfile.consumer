FROM python:3.12-slim

WORKDIR /app

# Install uv package manager
RUN pip install --no-cache-dir uv

# Start the consumer (dependencies installed via mounted volume)
CMD ["uv", "run", "python", "repo-scan.py"]
